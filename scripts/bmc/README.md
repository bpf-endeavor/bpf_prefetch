# About

The scripts are for doing experiments with BMC.
Make sure the memcached and bmc-binaries at `/others/` directory are prepared.

* `run_server`: run memcached and optionally attach bmc
	* --bmc-baseline: use baseline bmc
	* --bmc-batch: use batch aware bmc
